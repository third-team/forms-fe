import { SlideAnimation } from 'components';

import AnswerPassView from './AnswerPassView';

const AnswerPassViewSlideAnimationWrapper = (props) => <SlideAnimation component={AnswerPassView} {...props} />;

export default AnswerPassViewSlideAnimationWrapper;
