import { SlideAnimation } from 'components';

import QuestionContainer from './QuestionContainer';

const AnswerWrapper = (props) => <SlideAnimation component={QuestionContainer} {...props} />;

export default AnswerWrapper;
